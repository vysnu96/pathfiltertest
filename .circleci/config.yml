version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2023.02
    resource_class: small

    steps:     
      - run:
          name: conditional steps
          command: |
            if [ << pipeline.number >> == 1]; then
              echo "New Pipeline"
            elif
              echo "This is pipeline with number << pipeline.number >>"
        

  

workflows:
  demo:
    jobs:
      - build