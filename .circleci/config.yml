version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:2023.02
    resource_class: small

    steps:
      - run:
          name: Copying Files
          command: |
              echo $name
            

workflows:
  qa-deploy:
    jobs:
      - build:
          context: qa

  dev-deploy:
    jobs:
      - build:
          context: dev


