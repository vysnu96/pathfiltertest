version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:2023.02
    resource_class: small
    environment:
      NAME: tom
    steps:
      - run:
          name: Copying Files
          command: |
              if [ "${NAME}" == "tom" ]; then
                echo "If Worked
              else
                echo "Else worked"
              fi
            

workflows:
  qa-deploy:
    jobs:
      - build

