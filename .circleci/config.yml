version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2023.02
    resource_class: small

    steps:     
      - run: echo "Test Job"
      # - run:
      #     name: conditional steps
      #     command: |
      #       if [ << pipeline.number >> == 1]; then
      #         echo "New Pipeline"
      #       else
      #         echo "This is old pipeline with number << pipeline.number >> "
      #       fi
workflows:
  demo:
    when:
      or:
        - equal: [ test, << pipeline.git.tag >> ]
    jobs:
      - build