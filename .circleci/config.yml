version: 2.1
parameters:
  image-tag:
    type: string
    default: "latest"
 
jobs:
  build:
    docker:
      - image: cimg/node
        
    environment:
      IMAGETAG: << pipeline.parameters.image-tag >>
    
    steps:
      - run: echo "Image tag used was ${IMAGETAG}"