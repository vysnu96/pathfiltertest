version: 2.1
parameters:
  image-tag:
    type: string
    default: "latest"
 
jobs:
  build:
    machine:
      image: ubuntu-2004:202008-01
        
    steps:
      - checkout
      - run:
          name: Zipping
          command: | 
            zip list.zip list.txt
            ls -ltr

  

workflows:
  demo:
    jobs:
      - build